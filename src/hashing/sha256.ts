/**
 * The SHA256 hash of the given input, returned as a Uint8Array
 * storing the 32 bytes in big endian order.
 * @param input an input string to hash.
 */
export function SHA256(input: string): Uint8Array {
    throw new Error('not implemented');
}
