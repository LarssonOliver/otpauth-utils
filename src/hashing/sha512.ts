/**
 * The SHA512 hash of the given input, returned as a Uint8Array
 * storing the 64 bytes in big endian order.
 * @param input an input string to hash.
 */
export function SHA512(input: string): Uint8Array {
    throw new Error('not implemented');
}
